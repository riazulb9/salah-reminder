/* Moved. The city table now lives in src/main/cities-data.js so the main
   process can use it for first-run detection, and the settings window
   receives it over IPC. This file is no longer loaded. */
